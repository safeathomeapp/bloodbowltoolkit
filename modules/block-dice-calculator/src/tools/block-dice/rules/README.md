# Block Dice Rules Module

This folder will own the Blood Bowl block calculation logic for the MVP.

Rules code must:

- stay framework-independent
- return structured explanation data
- remain fully testable with Vitest

React components must not contain the block calculation rules themselves.
